drop job loadPayerTransitions

create loading job loadPayerTransitions for graph MedGraph {

    define filename f1;

    load f1 to edge PAYER_TRANSITION values ($3, $0, $1, $2, $4) using header="true";
}
