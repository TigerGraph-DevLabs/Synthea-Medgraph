drop job loadPatientNotes

create loading job loadPatientNotes for graph @graphname@ {

    define filename f1;

    load f1
        to vertex Notes values (gsql_concat($0, $1), $2, $3, $4, $5, $6, $7, $8, $1),
        to edge PATIENT_NOTE values($0, gsql_concat($0, $1))
        using header="true", separator=",";
}
