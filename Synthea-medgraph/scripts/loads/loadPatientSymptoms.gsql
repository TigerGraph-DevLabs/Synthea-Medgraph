drop job loadPatientSymptoms

create loading job loadPatientSymptoms for graph @graphname@ {

    define filename f1;

    load f1
        to vertex _Symptoms values($0, $8, $9, $7, $5, $6),
        to edge _PATIENT_HAS_SYMPTOM values($1, $0)
        using header="true", separator=",";
}
